<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>The Headphone Lab</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/css/bootstrap.min.css" rel="stylesheet" />
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

    <style>

        
  .carousel-item {
    transition: transform 1s ease-in-out !important;
  }



      body {
        font-family: "Georgia", "Garamond", "Times New Roman", serif;
        line-height: 1.6;
        background-color: #f9f9f9;
        margin: 0;
        padding: 20px;
       
      }

      .card {
        background-color: rgba(255, 255, 255, 0.95);
      }

      .main-image {
        text-align: center;
        margin-bottom: 30px;
      }

      .main-image img {
        max-width: 100%;
        height: auto;
        border-radius: 12px;
      }

      

      .dropdown-header {
        background-color: #007BFF;
        color: white;
        padding: 8px 16px;
        margin: 0;
      }

      .review, .spec, .external-links, .buy-links {
        background-color: white;
        padding: 20px;
        margin-bottom: 20px;
        border-radius: 10px;
        box-shadow: 0 1px 5px rgba(0,0,0,0.1);
      }

      .button-link {
        display: inline-block;
        margin: 10px 10px 0 0;
        padding: 10px 15px;
        background-color: #333;
        color: white;
        text-decoration: none;
        border-radius: 6px;
      }

      .button-link:hover {
        background-color: #555;
      }

      .spec-title {
        background-color: #007BFF;
        color: white;
        padding: 12px 20px;
        border-radius: 6px;
        font-size: 20px;
        margin-top: 30px;
        margin-bottom: 15px;
      }
    </style>

    @if($slots.style)
      {{{ await $slots.style() }}}
    @endif
  </head>

  <body>
    @if($slots.navbar)
      {{ await $slots.navbar() }}
    @else
      @include('partials/navbar')
    @end

    @if($slots.sidebar)
      {{ await $slots.sidebar() }}
    @else
      @include('partials/sidebar')
    @end

    <div class="container my-4">
      {{{ await $slots.main() }}}
    </div>

    @if($slots.script)
      {{{ await $slots.script() }}}
    @end
    <script
    src="https://cdn.jsdelivr.net/npm/jquery@3.6.1/dist/jquery.min.js"
  ></script>
  <script
    src="https://cdn.jsdelivr.net/npm/semantic-ui@2.4.2/dist/semantic.min.js"
  ></script>
    <script>
  const formats = ['webp', 'jpg', 'png'];

  document.querySelectorAll('img[id^="img-"]').forEach(imgEl => {
    const name = imgEl.id.replace('img-', '');

    let found = false;

    formats.forEach(ext => {
      if (found) return;
      const img = new Image();
      img.src = `/${name}.${ext}`;

      img.onload = () => {
        imgEl.src = img.src;
        found = true;

      };
    });
  });
</script>
<script>
  document.addEventListener('DOMContentLoaded', () => {
    document.querySelectorAll('.toggle-edit').forEach((btn) => {
      btn.addEventListener('click', () => {
        const id = btn.getAttribute('data-id')
        const form = document.getElementById(`edit-form-${id}`)
        if (form.style.display === 'none') {
          form.style.display = 'block'
        } else {
          form.style.display = 'none'
        }
      })
    })
  })
</script>

  </body>
</html>
