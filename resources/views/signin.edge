<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Sign In</title>
  <link
    rel="stylesheet"
    href="https://cdn.jsdelivr.net/npm/semantic-ui@2.4.2/dist/semantic.min.css"
  >
  <style>
    /* Full viewport centering and background image */
    body, html {
      height: 100%;
      margin: 0;
    }
    .auth-wrapper {
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100%;
      background: url('/loginvdo.jpg') center center / cover no-repeat fixed;
    }
    .auth-box {
      background: rgba(255, 255, 255, 0.9);
      padding: 2em;
      box-shadow: 0 2px 12px rgba(0, 0, 0, 0.2);
      border-radius: 8px;
      width: 360px;
    }
    .auth-header {
      text-align: center;
      margin-bottom: 1.5em;
    }
    .register-link {
      display: block;
      text-align: center;
      margin-top: 1em;
      color: #4183c4;
    }
  </style>
  <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
</head>
<body>
  <div class="auth-wrapper">
    <div class="auth-box">
      <h1 class="ui header auth-header">Sign In to Your Account</h1>

      <form class="ui form" action="/login" method="post">
        {{ csrfField() }}

        <div class="field">
          <label>Username</label>
          <div class="ui left icon input">
            <input type="text" name="username" placeholder="Username" required>
            <i class="user icon"></i>
          </div>
        </div>

        <div class="field">
          <label>Password</label>
          <div class="ui left icon input">
            <input type="password" name="password" placeholder="Password" required>
            <i class="lock icon"></i>
          </div>
        </div>

        <button class="ui fluid large primary button" type="submit">Login</button>
      </form>

      <a href="/register" class="register-link">Don't have an account? Register</a>
    </div>
  </div>
</body>
</html>
